# User Project Workspace

This directory is reserved for user-created project artifacts.

Generated artifacts can be organized in per-project subdirectories when a workflow needs durable intermediate files or exports. Keep large outputs out of source control unless they are intentional examples.

## Notes

- Contents under this directory are excluded by `.gitignore`.
- Completed reusable examples can be moved to an examples directory for sharing.